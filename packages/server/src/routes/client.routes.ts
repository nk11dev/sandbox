/**
 * List of client-side routes for the SPA.
 * These routes will serve the index.html file, 
 * allowing React Router to handle routing on the client.
 */
export const clientRoutes = [
    '/',
    '/users',
    '/roles',
    '/groups',
    '/groups/:id',
]
