import { observer } from 'mobx-react'

import './Footer.css'

/**
 * Application footer.
 */
export const Footer = observer(function Footer() {
    return (
        <footer className="footer">
            {/* Empty footer with just border */}
        </footer>
    )
})
