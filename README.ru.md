# Sandbox - Интеграция MobX + TanStack Query

Fullstack песочница, демонстрирующая интеграцию **MobX** и **TanStack Query** с поддержкой транспортов **HTTP** и **WebSocket**.

## 🚀 Быстрый старт

### Установка

```bash
# Установить все зависимости для всех пакетов
npm run install:all
```

### Разработка

```bash
# Терминал 1: Запустить сервер (режим разработки)
npm run dev:server

# Терминал 2: Запустить клиент (режим разработки)
npm run dev:client
```

- Сервер: http://localhost:5000
- Клиент: http://localhost:3000
- API: http://localhost:5000/api

### Production сборка

```bash
# Собрать и запустить в production режиме
npm run prod
```

Сервер будет отдавать API и статические файлы клиента на http://localhost:5000

## 📁 Структура проекта

```
sandbox/
├── packages/
│   ├── common/          # Общие типы и утилиты
│   │   ├── types/       # UserDto, RoleDto, GroupDto, AccessDto
│   │   └── utils/       # Тип ApiResponse
│   ├── server/          # Express + Socket.io бэкенд
│   │   ├── controllers/ # CRUD обработчики для всех сущностей
│   │   ├── db/          # LowDB с JSON хранилищем
│   │   └── routes/      # HTTP REST + WebSocket обработчики
│   └── client/          # React + MobX + TanStack Query фронтенд
│       ├── services/    # HttpApi, WebSocketApi, MobxQuery, MobxMutation
│       ├── stores/      # Entity и State хранилища
│       │   ├── entities/  # Слой данных (Users, Roles, Groups, Access)
│       │   └── state/     # Слой бизнес-логики
│       └── components/  # React UI компоненты
│           ├── users/     # Страница пользователей (HTTP + WebSocket)
│           ├── roles/     # Страница ролей (HTTP + WebSocket)
│           ├── groups/    # Каталог групп + Матрица доступа
│           └── layout/    # Header, Footer, Breadcrumbs
├── .env.defaults        # Переменные окружения по умолчанию
└── package.json         # Конфигурация корневого workspace
```

## 📄 Реализованные страницы

### Страница пользователей (`/users`)
- Бок-о-бок реализации HTTP и WebSocket
- CRUD операции для пользователей
- Синхронизация в реальном времени между блоками
- **Демонстрирует:** Кейсы 1, 2, 3, 4, 5, 6

### Страница ролей (`/roles`)
- Бок-о-бок реализации HTTP и WebSocket
- CRUD операции для ролей
- Синхронизация в реальном времени между блоками
- Кросс-сущностная инвалидация (роли → группы)
- **Демонстрирует:** Кейсы 1, 2, 3, 4, 5, 6

### Страница групп (`/groups`)
- **Каталог групп:** CRUD с мультиселектом ролей
- **Матрица доступа:** Права пользователь-группа с чекбоксами
- Поиск в реальном времени (на основе кэша, без API вызовов)
- Автоматические обновления при изменении групп/пользователей
- **Демонстрирует:** Все кейсы + продвинутые паттерны

### Страница профиля группы (`/groups/:id`)
- Редактирование отдельной группы с мультиселектом ролей
- Загрузка данных из кэша в первую очередь
- Детектирование несохраненных изменений
- **Демонстрирует:** Кейсы 1, 4, 5

## 🎯 Ключевые возможности

### Кейс 1: MobX + TanStack Query + HTTP
- Entity хранилища используют `MobxQuery` для реактивной загрузки данных
- Автоматическое обновление кэша через `MobxMutation`
- Типобезопасность TypeScript во всей цепочке
- **Реализовано в:** Entity сущностей Users, Roles, Groups, Access

### Кейс 2: MobX + TanStack Query + WebSocket
- Тот же паттерн что и HTTP, но с Socket.io транспортом
- Обновления в реальном времени через WebSocket события
- Автоматическая инвалидация кэша по событиям
- **Реализовано в:** Entity сущностей Users, Roles (Groups и Access готовы)

### Кейс 3: Транспортно-агностичные компоненты
- Один компонент работает с HTTP и WebSocket
- Внедрение зависимостей через state prop
- Полиморфизм на основе интерфейсов
- **Реализовано в:** `UsersList.tsx`, `RolesList.tsx`

### Кейсы 4-6: Мутации и кэширование
- **Кейс 4**: Оптимистичные обновления с `onSuccess` колбэками
- **Кейс 5**: Чтение из кэша без дополнительных запросов
- **Кейс 6**: Кросс-сущностные цепочки инвалидации кэша

### Продвинутые паттерны
- **Мульти-сущностный State**: `GroupsPageState` объединяет 4 entity хранилища
- **Загрузка из кэша сначала**: Проверка кэша перед запросом к серверу
- **Поиск в реальном времени**: Фильтрация кэшированных данных без API вызовов
- **Коллаборативное редактирование**: Матрица доступа с синхронизацией в реальном времени
- **Кросс-сущностные зависимости**: Цепочка инвалидации Роли → Группы → Доступ

## 🧪 Тестирование интеграции

### Сценарий 1: HTTP к WebSocket синхронизация

**Страница:** Users или Roles
1. Открыть страницу в браузере
2. Создать элемент через блок "HTTP"
3. ✅ Оба блока HTTP и WebSocket автоматически обновляются

### Сценарий 2: Обновления в реальном времени

**Страница:** Users или Roles
1. Открыть страницу в двух вкладках браузера
2. Создать/обновить/удалить элемент в любой вкладке
3. ✅ Изменения появляются в обеих вкладках мгновенно

### Сценарий 3: Независимость от транспорта

**Реализация:**
1. Сравнить `UsersEntityHttp` с `UsersEntityWebSocket`
2. Сравнить `UsersListStateHttp` с `UsersListStateWebSocket`
3. ✅ Одинаковые паттерны, разные транспорты
4. ✅ Компонент `UsersList` не зависит от транспорта

### Сценарий 4: Кросс-сущностная инвалидация кэша

**Страница:** Groups
1. Создать роль на странице Roles
2. Перейти на страницу Groups → Создать группу с этой ролью
3. Удалить роль на странице Roles
4. ✅ Страница Groups автоматически перезагружается (инвалидация кэша)

### Сценарий 5: Загрузка из кэша сначала

**Страница:** Group Profile
1. Посетить страницу Groups (группы закэшированы)
2. Кликнуть на имя группы для открытия профиля
3. ✅ Данные группы загружаются мгновенно из кэша
4. Проверить логи консоли на сообщение "Found in cache"

### Сценарий 6: Поиск в реальном времени без запросов

**Страница:** Groups (Матрица доступа)
1. Ввести имя пользователя в поле поиска
2. ✅ Матрица фильтруется мгновенно без API вызовов
3. Очистить поиск
4. ✅ Полная матрица появляется мгновенно (из кэша)

### Сценарий 7: Коллаборативная матрица доступа

**Страница:** Groups (Матрица доступа)
1. Открыть страницу Groups в двух вкладках браузера
2. Переключить чекбокс доступа в первой вкладке
3. ✅ Вторая вкладка обновляется автоматически в реальном времени
4. Проверить консоль на WebSocket события

### Сценарий 8: Управление мульти-сущностным состоянием

**Страница:** Groups
1. Создать/удалить группы в каталоге (сверху)
2. ✅ Столбцы матрицы доступа (снизу) обновляются автоматически
3. Создать/удалить пользователей на странице Users
4. ✅ Строки матрицы доступа обновляются автоматически

## 📚 Архитектура

### Слой Entity
- Управляет загрузкой данных и мутациями
- Использует обертки `MobxQuery` и `MobxMutation`
- Единственный источник истины для каждой сущности
- Специфичен для транспорта (HTTP или WebSocket)

### Слой State
- Управляет UI состоянием (модалки, формы, выборки)
- Использует Entity хранилища для доступа к данным
- Предоставляет computed свойства компонентам
- Содержит бизнес-логику

### Слой Component
- Чистые React компоненты с MobX observer
- Получают state через props (внедрение зависимостей)
- Не зависят от транспорта при использовании интерфейсов
- Фокусируются на представлении

## 🔧 Доступные скрипты

```bash
npm run dev:client      # Запустить dev сервер клиента
npm run dev:server      # Запустить dev сервер сервера
npm run build           # Собрать клиент и сервер
npm run build:client    # Собрать только клиент
npm run build:server    # Собрать только сервер
npm run start           # Запустить production сервер
npm run prod            # Собрать и запустить production
npm run lint            # Линтовать все пакеты
npm run lint:fix        # Линтовать и исправить все пакеты
npm run ts:check        # TypeScript проверка всех пакетов
npm run test            # Запустить тесты (пакет client)
```

## 🧪 Тестирование

Проект включает комплексные тесты для всех паттернов интеграции:

```bash
# Запустить все тесты
npm test

# Запустить тесты с покрытием
npm run test:coverage

# Запустить тесты в режиме отслеживания
npm run test:watch
```

### Покрытие тестами

- ✅ **Entity Stores**: Слои транспорта HTTP и WebSocket
- ✅ **State Stores**: Управление UI состоянием и бизнес-логика
- ✅ **Components**: Транспортно-агностичные UI компоненты
- ✅ **Integration**: End-to-end сценарии тестирующие все 6 кейсов

### Структура тестов

```
packages/client/src/
├── __tests__/
│   ├── testUtils.tsx           # Тестовые утилиты и моки
│   └── integration/            # Интеграционные тесты
│       └── usersFlow.test.tsx
├── stores/
│   ├── entities/__tests__/    # Тесты Entity слоя
│   │   ├── UsersEntityHttp.test.ts
│   │   └── UsersEntityWebSocket.test.ts
│   └── state/__tests__/       # Тесты State слоя
│       └── UsersListStateHttp.test.ts
└── components/
    └── users/__tests__/       # Тесты компонентов
        └── UsersList.test.tsx
```

## 🌐 Переменные окружения

См. `.env.defaults` для доступных переменных:

```env
PORT_CLIENT=3000
PORT_SERVER=5000
API_HOST=
API_PATH=/api
```

Создайте файл `.env` для переопределения значений по умолчанию.

## 🛠️ Технологический стек

### Фронтенд
- React 18.2.0
- MobX 6.12.0
- TanStack Query 5.90.16
- Socket.io Client 4.8.1
- Vite 5.4.19
- TypeScript 5.4.2

### Бэкенд
- Express 4.18.2
- Socket.io 4.8.1
- LowDB 7.0.1
- TypeScript 5.4.2

## 📖 Документация

Для детальной архитектуры и деталей реализации смотрите:
- `ARCHITECTURE.md` / `ARCHITECTURE.ru.md` - Паттерны интеграции и лучшие практики
- `TESTING.md` / `TESTING.ru.md` - Руководство по тестированию
- Комментарии в коде Entity и State хранилищ
- JSDoc документация компонентов

## 🎨 Стиль кода

- Отступы 4 пробела
- Без точек с запятой
- Одинарные кавычки
- Максимальная длина строки: 100 символов
- ESLint + TypeScript строгий режим

## 📝 Лицензия

MIT
